import os
dirs = os.listdir("/home/bluefrog/sample_python/python")
Filepaths = []
for subdir in dir:
    filename = os.path.join(your path, subdir)
    if os.path.isfile(filename):
        filepaths.append(filename)

for i in range(len(filepaths)):
    filepaths[i] = (filepaths[i] , os.path.getsize(filepath(I)))

